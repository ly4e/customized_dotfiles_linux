- defaults:
    link:
      relink: true

- clean: ['~']

- link:
#    ~/.vim: vim
    ~/.tmux.conf: tmux/tmux.conf
    ~/.zshrc: zshrc

- create:
    - ~/Work
    - ~/zzz/zzzz/asdf
#    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
