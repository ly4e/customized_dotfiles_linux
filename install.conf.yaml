- defaults:
    link:
      relink: true

- clean: ['~']

#- link:
#    ~/.tmux.conf: tmux.conf
#    ~/.vim: vim
#    ~/.vimrc: vimrc

- create:
    - ~/Work
    - ~/zzz/zzzz/asdf
#    - ~/.vim/undo-history

#- shell:
#  - [git submodule update --init --recursive, Installing submodules]
